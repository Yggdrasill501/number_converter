#include <iostream>
#include "converter.h"

int main() {
    // chose if you want to convert from decimal to hexadecimal or from hexadecimal to decimal
    // input number
    // print result
    Converter converter;
    converter.userInterface();

    return 0;
}
